VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_API_URL=https://your-backend.railway.app

# Sem chave de API aqui, de propósito: variável VITE_* é compilada DENTRO do
# JavaScript, e o bundle é público. A calculadora interna autentica por sessão
# (exige login); a chave dos embeds do Ploomes chega em execução, do script do
# campo desenvolvedor.
